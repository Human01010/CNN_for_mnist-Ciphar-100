mnist_MLP.py for mission1: using module MLP and dataset mnist. 
                           the dataset can be downloaded with package torchvision, datasets. 
                           the dataset's address is .../data/MNIST
                                                            /mnist_MLP.py
                            if you use relative address or you can use absolute address.

mnist_CNN2.py for mission2: using CNN with almost the same params of MLP in mission1. 
                            the dataset is the same, mnist.

other python files for mission3: using almost 7 layers CNN network with classic backbone. 
                            dataset: ciphar-100, you can downlaod it using package: torchvision, datasets
                            or you can downlaod it via the following link: https://www.cs.toronto.edu/~kriz/cifar-100-python.tar.gz
                            the relative address is nearly the same as misson1, or you can use absolute address as you wish.

you can get more information via the following link:
